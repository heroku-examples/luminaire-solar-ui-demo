import { createTheme } from '@mantine/core';

export const title = 'Lumina Solar Inc.';
export const year = '2024';

export const theme = createTheme({
  fontFamily: 'Inter, sans-serif',
  primaryColor: 'violet',
});
