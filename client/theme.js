import { createTheme } from '@mantine/core';

export const title = 'Hi Austin';
export const year = '2024';

export const theme = createTheme({
  fontFamily: 'Inter, sans-serif',
  primaryColor: 'violet',
});
