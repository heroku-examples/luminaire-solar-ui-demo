import { createTheme } from '@mantine/core';

export const title = 'Luminaire Solar Oh Yeah';
export const year = '2024';

export const theme = createTheme({
  fontFamily: 'Inter, sans-serif',
  primaryColor: 'violet',
});
